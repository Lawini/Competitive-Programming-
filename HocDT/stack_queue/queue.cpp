#include <stdio.h>
#include <math.h>
#include <limits.h>
void push (int x){
	a[++e] = x;
}
void pop(){
	s++
	return a[s - 1];
}

int at (int i){
	if (s <= i && i <= e) return a[i];
	else return NULL;
}

int main(){
	
	
	int n;
	scanf("%d", &n);
	push (n);
	
	
	
}
